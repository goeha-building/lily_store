import { useEffect, useRef, useState, type ChangeEvent, type FormEvent } from 'react';
import type { Coupon, DashboardStats, ProductInput, Recommendation, TuckShopProduct } from '../types';
import './AdminDashboard.css';

interface Props {
  products: TuckShopProduct[]; stats: DashboardStats; coupons: Coupon[]; recommendations: Recommendation[];
  onCreateCoupon: (amount: number, expiresAt: string) => Promise<void>;
  onAddProduct: (input: ProductInput) => Promise<void>;
  onRestock: (product: TuckShopProduct, amount: number) => Promise<void>;
  onUpdateProduct: (id: string, updates: Pick<TuckShopProduct, 'name' | 'category' | 'price' | 'currentStock' | 'aliases'>) => Promise<void>;
  onDeleteProduct: (id: string) => Promise<void>;
  onCompleteRecommendation: (item: Recommendation) => Promise<void>;
  onDeleteRecommendation: (id: string) => Promise<void>;
  onExit: () => void;
}

const PAGE_SIZE = 5;
const initial: ProductInput = { name: '', aliasesText: '', category: '음료', price: 0, stockDate: new Date().toISOString().slice(0, 10), stockQuantity: 1 };

function Pager({ page, total, onChange }: { page: number; total: number; onChange: (page: number) => void }) {
  const pages = Math.max(1, Math.ceil(total / PAGE_SIZE));
  return <div className="compact-pager"><button type="button" aria-label="이전 페이지" disabled={page === 0} onClick={() => onChange(page - 1)}>‹</button><span>&lt;{page + 1}/{pages}&gt;</span><button type="button" aria-label="다음 페이지" disabled={page + 1 >= pages} onClick={() => onChange(page + 1)}>›</button></div>;
}

export default function AdminDashboard(props: Props) {
  const [form, setForm] = useState<ProductInput>(initial); const [message, setMessage] = useState(''); const [editing, setEditing] = useState<TuckShopProduct | null>(null);
  const [couponAmount, setCouponAmount] = useState(1000); const [couponExpiry, setCouponExpiry] = useState(new Date().toISOString().slice(0, 10));
  const [couponPage, setCouponPage] = useState(0); const [stockPage, setStockPage] = useState(0); const [copiedCouponId, setCopiedCouponId] = useState<string | null>(null); const fileRef = useRef<HTMLInputElement>(null);
  const coupons = props.coupons.slice(couponPage * PAGE_SIZE, (couponPage + 1) * PAGE_SIZE); const products = props.products.slice(stockPage * PAGE_SIZE, (stockPage + 1) * PAGE_SIZE);
  useEffect(() => setCouponPage((page) => Math.min(page, Math.max(0, Math.ceil(props.coupons.length / PAGE_SIZE) - 1))), [props.coupons.length]);
  useEffect(() => setStockPage((page) => Math.min(page, Math.max(0, Math.ceil(props.products.length / PAGE_SIZE) - 1))), [props.products.length]);
  const change = <K extends keyof ProductInput>(key: K, value: ProductInput[K]) => setForm((current) => ({ ...current, [key]: value }));
  const submit = async (event: FormEvent) => { event.preventDefault(); try { await props.onAddProduct(form); setForm(initial); setMessage('상품을 등록했습니다.'); } catch (error) { setMessage(error instanceof Error ? error.message : '상품 등록에 실패했습니다.'); } };
  const createCoupon = async (event: FormEvent) => { event.preventDefault(); try { await props.onCreateCoupon(couponAmount, couponExpiry); setCouponPage(0); setMessage('쿠폰을 발급했습니다.'); } catch (error) { setMessage(error instanceof Error ? error.message : '쿠폰 발급에 실패했습니다.'); } };
  const upload = async (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file) return; try { const rows = (await file.text()).replace(/^\uFEFF/, '').trim().split(/\r?\n/).slice(1); for (const row of rows) { const [name, category, price, stockDate, stockQuantity, aliasesText = '', calories = ''] = row.split(',').map((cell) => cell.trim().replace(/^"|"$/g, '')); if (name && category && price && stockQuantity) await props.onAddProduct({ name, category, price: Number(price), stockDate: stockDate || initial.stockDate, stockQuantity: Number(stockQuantity), aliasesText, calories: calories ? Number(calories) : undefined }); } setMessage('CSV 상품 등록을 완료했습니다.'); } catch (error) { setMessage(error instanceof Error ? error.message : 'CSV 등록에 실패했습니다.'); } if (fileRef.current) fileRef.current.value = ''; };
  const copyCoupon = async (coupon: Coupon) => { await navigator.clipboard.writeText(coupon.serialNumber); setCopiedCouponId(coupon.id); window.setTimeout(() => setCopiedCouponId(null), 1500); };
  const pending = props.recommendations.filter((item) => item.status !== 'completed');
  return <main className="admin-shell"><header className="admin-header"><div><p className="eyebrow">ADMIN DASHBOARD</p><h1>매점 관리</h1></div><button className="secondary-button" onClick={props.onExit}>사용자 화면으로</button></header>
    <section className="stats-grid"><article><small>등록 사용자</small><b>{props.stats.userCount}명</b></article><article><small>오늘 구매</small><b>{props.stats.todayPurchaseCount}건</b></article><article><small>이번 주 구매</small><b>{props.stats.weeklyPurchaseCount}건</b></article></section>
    <section className="admin-form coupon-admin"><h2>쿠폰 발급</h2><form onSubmit={createCoupon}><label>쿠폰 금액<input type="number" min="1" value={couponAmount} onChange={(event) => setCouponAmount(Number(event.target.value))} /></label><label>만료일<input type="date" value={couponExpiry} onChange={(event) => setCouponExpiry(event.target.value)} /></label><button className="primary-button">쿠폰 발급</button></form><div className="list-heading"><h3>발급 쿠폰 · 최신순</h3><Pager page={couponPage} total={props.coupons.length} onChange={setCouponPage} /></div><ul className="coupon-admin-list">{coupons.map((coupon) => <li key={coupon.id}><div className="coupon-serial"><b>{coupon.serialNumber}</b><span>발급 {coupon.createdAt?.toLocaleDateString('ko-KR') ?? '-'}</span></div><div className="coupon-info"><strong className="coupon-price">{coupon.currentBalance.toLocaleString()}원</strong><span className={`coupon-status status-${coupon.status}`}>{coupon.status}</span></div><button type="button" className={copiedCouponId === coupon.id ? 'copy-button copied' : 'copy-button'} onClick={() => void copyCoupon(coupon)}>{copiedCouponId === coupon.id ? '복사됨' : '복사'}</button></li>)}</ul></section>
    <section className="admin-products recommendation-admin"><div className="admin-section-title"><div><p className="eyebrow">STUDENT FEEDBACK</p><h2>입고 희망 상품</h2></div><b>{pending.length}건</b></div><ul>{pending.length ? pending.map((item) => <li key={item.id}><div><strong>{item.name}</strong><p>공감 {item.supporters.length}명</p></div><div className="recommendation-actions"><button onClick={() => void props.onCompleteRecommendation(item)}>입고 완료</button><button onClick={() => { if (window.confirm(`${item.name} 요청을 삭제할까요?`)) void props.onDeleteRecommendation(item.id); }}>삭제</button></div></li>) : <li className="empty-message">대기 중인 입고 희망 상품이 없습니다.</li>}</ul></section>
    <div className="admin-grid"><form className="admin-form" onSubmit={submit}><h2>새 상품 입고</h2><label className="excel-button">CSV 일괄 업로드<input ref={fileRef} type="file" accept=".csv,text/csv" onChange={(event) => void upload(event)} hidden /></label><label>상품명<input required value={form.name} onChange={(event) => change('name', event.target.value)} /></label><label>검색 별칭<input value={form.aliasesText} onChange={(event) => change('aliasesText', event.target.value)} /></label><div className="form-columns"><label>종류<select value={form.category} onChange={(event) => change('category', event.target.value)}><option>음료</option><option>과자</option><option>빵</option><option>캔디</option><option>기타</option></select></label><label>가격<input required type="number" min="0" value={form.price || ''} onChange={(event) => change('price', Number(event.target.value))} /></label></div><div className="form-columns"><label>입고일<input required type="date" value={form.stockDate} onChange={(event) => change('stockDate', event.target.value)} /></label><label>입고 수량<input required type="number" min="1" value={form.stockQuantity || ''} onChange={(event) => change('stockQuantity', Number(event.target.value))} /></label></div><button className="primary-button">상품 등록</button><p className="admin-message">{message}</p></form>
      <section className="admin-products"><div className="list-heading"><h2>재고 현황 ({props.products.length})</h2><Pager page={stockPage} total={props.products.length} onChange={setStockPage} /></div><ul>{products.map((product) => <li key={product.id}><div><strong>{product.name}</strong><p>{product.category} · {product.price.toLocaleString()}원</p><small>재고 {product.currentStock}</small></div><div className="stock"><button onClick={() => setEditing(product)}>수정</button><button onClick={() => { if (window.confirm(`${product.name} 상품을 삭제할까요?`)) void props.onDeleteProduct(product.id).catch((error: unknown) => setMessage(error instanceof Error ? error.message : '삭제 실패')); }}>삭제</button><button onClick={() => { const amount = Number(window.prompt('추가 입고 수량', '1')); if (amount > 0) void props.onRestock(product, amount); }}>+ 입고</button></div></li>)}</ul></section></div>
    {editing && <EditModal product={editing} onClose={() => setEditing(null)} onSave={async (updates) => { await props.onUpdateProduct(editing.id, updates); setEditing(null); }} />}
  </main>;
}

function EditModal({ product, onClose, onSave }: { product: TuckShopProduct; onClose: () => void; onSave: (updates: Pick<TuckShopProduct, 'name' | 'category' | 'price' | 'currentStock' | 'aliases'>) => Promise<void> }) { const [name, setName] = useState(product.name); const [category, setCategory] = useState(product.category); const [price, setPrice] = useState(product.price); const [stock, setStock] = useState(product.currentStock); const [aliases, setAliases] = useState(product.aliases.join(', ')); return <div className="modal-backdrop"><form className="success-modal edit-modal" onSubmit={(event) => { event.preventDefault(); void onSave({ name, category, price, currentStock: stock, aliases: aliases.split(',').map((item) => item.trim()).filter(Boolean) }); }}><h2>상품 수정</h2><label>상품명<input value={name} onChange={(event) => setName(event.target.value)} /></label><label>종류<input value={category} onChange={(event) => setCategory(event.target.value)} /></label><label>가격<input type="number" min="0" value={price} onChange={(event) => setPrice(Number(event.target.value))} /></label><label>현재 재고<input type="number" min="0" value={stock} onChange={(event) => setStock(Number(event.target.value))} /></label><label>별칭<input value={aliases} onChange={(event) => setAliases(event.target.value)} /></label><div className="modal-actions"><button type="button" className="secondary-button" onClick={onClose}>취소</button><button className="primary-button">저장</button></div></form></div>; }
